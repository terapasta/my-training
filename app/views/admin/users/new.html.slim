- content_for(:html_title) { t('admin.users.new.title') }
.columns.is-centered
  .column.is-8-desktop
    h1.is-size-3 = t('admin.users.new.title')
    hr
    .
      nav.level
        .level-left
        .level-right
          .level-item
            = render partial: 'admin/users/shared/links/index'
      .card-background
        = render partial: 'admin/users/shared/forms/form', locals: { user: @user, btn_name: t('buttons.create') }
