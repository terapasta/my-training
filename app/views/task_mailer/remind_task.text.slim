= "#{@user.name}さん"

= create_remind_message(@task)


= "#{t('activerecord.attributes.task.name')}: #{@task.name}"

= "#{t('activerecord.attributes.task.deadline')}: #{@task.deadline}"

= "#{t('activerecord.attributes.task.debtee')}: #{@task.get_debtee.name}"

= "#{t('activerecord.attributes.task.description')}: #{@task.description}"
